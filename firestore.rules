rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Barcha collectionlarga faqat autentifikatsiyadan o'tgan userlar kira oladi
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
